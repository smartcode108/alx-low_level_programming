#include "holberton.h"

/**
* set_string - function that sets the value of a pointer to a char
* @s: pointer to the address of strings
* @to: strings to set value to
*
Return: nothing
*/
void set_string(char**s,char*to)
{
*s= to;
}
